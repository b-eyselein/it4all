{
  "title": "Quiz",
  "type": "object",
  "properties": {
    "id": {"type": "number"},
    "semanticVersion": {"$ref": "#/definitions/SemanticVersion"},
    "title": {"type": "string"},
    "author": {"type": "string"},
    "text": {"type": "string"},
    "status": {"type": "string", "enum": ["RESERVED", "CREATED", "ACCEPTED", "APPROVED"]},
    "theme": {"type": "string"},
    "exercises": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/Question"
      }
    }
  },
  "required": ["id", "semanticVersion", "title", "author", "text", "status", "theme", "exercises"],
  "definitions": {
    "Question": {
      "type": "object",
      "properties": {
        "id": {"type": "number"},
        "title": {"type": "string"},
        "author": {"type": "string"},
        "text": {"type": "string"},
        "maxPoints": {"type": "number"},
        "exerciseType": {"type": "string", "enum": ["CHOICE", "FILLOUT"]},
        "status": {"type": "string", "enum": ["RESERVED", "CREATED", "ACCEPTED", "APPROVED"]},
        "semanticVersion": {"$ref": "#/definitions/SemanticVersion"},
        "answers": {
          "type": "array",
          "items": {"$ref": "#/definitions/Answer"}
        }
      },
      "required": ["id", "title", "author", "text", "maxPoints", "exerciseType", "status", "semanticVersion", "answers"]
    },
    "Answer": {
      "type": "object",
      "properties": {
        "id": {"type": "number"},
        "text": {"type": "string"},
        "correctness": {"type": "string", "enum": ["WRONG", "OPTIONAL", "CORRECT"]},
        "explanation": {"type": "string"}
      },
      "required": ["id", "text", "correctness"]
    },
    "SemanticVersion": {
      "anyOf": [
        {"type": "string"},
        {
          "type": "object",
          "properties": {
            "major": {"type": "number"},
            "minor": {"type": "number"},
            "patch": {"type": "number"}
          }
        }
      ]
    }
  }
}