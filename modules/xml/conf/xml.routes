GET    /exercises   controllers.xml.XML.index()

GET    /:ex         controllers.xml.XML.exercise(ex: Int)

PUT    /:ex         controllers.xml.XML.commit(ex: Int)
POST   /:ex         controllers.xml.XML.commit(ex: Int)