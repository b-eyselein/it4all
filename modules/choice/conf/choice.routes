# creation of exercises
GET    /upload        controllers.choice.ChoiceAdmin.uploadForm()
POST   /upload        controllers.choice.ChoiceAdmin.uploadFile()
GET    /create        controllers.choice.ChoiceAdmin.readStandardExercises()

#
GET    /              controllers.choice.ChoiceController.index()
GET    /:quizId/:id   controllers.choice.ChoiceController.question(quizId: Int, id: Int)

POST   /:quizId/:id   controllers.choice.ChoiceController.correct(quizId: Int, id: Int)