// docker: Cannot connect to the Docker daemon. Is the docker daemon running on this host?.
sudo usermod -aG docker $(whoami)

First db (it4all: Ebean):
docker run --name mysql1 -e MYSQL_ROOT_PASSWORD=sT8aV#k7 -e MYSQL_DATABASE=it4all -p 3306:3306 --restart=always -d mysql:5.7

Second db (SQL-Select):
docker run --name mysql2 -e MYSQL_ROOT_PASSWORD=sT8aV#k7 -e MYSQL_DATABASE=it4all -p 3307:3306 --restart=always -d mysql:5.7

Third db (SQL-Other):
docker run --name mysql3 -e MYSQL_ROOT_PASSWORD=sT8aV#k7 -e MYSQL_DATABASE=it4all -p 3308:3306 --restart=always -d mysql:5.7

Run on all databases (2 commands):
GRANT ALL PRIVILEGES ON it4all.* TO 'it4all'@'%';
FLUSH PRIVILEGES;
